import { } from "../actionTypes.js"

const initialState = {
}

function apiReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}

export default apiReducer